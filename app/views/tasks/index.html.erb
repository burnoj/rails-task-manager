<h1>All Tasks</h1>

<% @tasks.each do |t|  %>
  <% if t.completed == true %>
    <p> [X] <%=link_to "#{t.title}", task_path(t.id)%>  </p>
  <% else %>
    <p> "[ ] <%= link_to "#{t.title}", task_path(t.id)%> </p>
  <% end %>
<% end %>

<h2><%= link_to "Add a new Task", tasks_new_path %></h2>

